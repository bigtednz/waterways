{
  "name": "big-teds-sports-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "npm run build:packages && npm run build:apps",
    "build:manual": "npm run build:packages && npm run build:apps",
    "build:fix-esbuild": "node node_modules/vite/node_modules/esbuild/install.js",
    "build:packages": "npm run build --workspace=@waterways/shared && npm run build --workspace=@waterways/db && npm run build --workspace=@waterways/analytics-engine",
    "build:apps": "npm run build --workspace=@waterways/api && npm run build --workspace=@waterways/web",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "test": "turbo run test",
    "db:generate": "cd packages/db && npm run generate",
    "db:migrate": "cd packages/db && npm run migrate",
    "db:migrate:deploy": "cd packages/db && npm run migrate:deploy",
    "db:seed": "cd packages/db && npm run seed",
    "db:studio": "cd packages/db && npm run studio"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^1.11.3",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
